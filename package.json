{
  "name": "probono",
  "version": "1.0.0",
  "private": true,
  "description": "Probono Monorepo",
  "workspaces": [
    "backend",
    "fe"
  ],
  "scripts": {
    "backend:dev": "npm run start:dev -w backend",
    "backend:build": "npm run build -w backend",
    "backend:prisma:generate": "npm run prisma generate -w backend",
    "fe:dev": "npm run dev -w fe",
    "install:all": "npm install"
  },
  "devDependencies": {
    "prettier": "^3.4.2"
  }
}
